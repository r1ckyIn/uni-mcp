{
  "name": "uni-mcp-dev",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only dependencies for scripts/validate.sh (issue #3). The plugin itself ships no runtime code; this file never reaches installed plugins (node_modules is gitignored, the manifest is .claude-plugin/plugin.json).",
  "devDependencies": {
    "markdownlint-cli2": "0.23.2",
    "remark-cli": "12.0.1",
    "remark-validate-links": "13.1.0"
  }
}
